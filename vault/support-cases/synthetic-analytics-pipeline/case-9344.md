---
layer: support-case
tags: [load, duplicate]
created: 2026-07-02
links:
  - "[[structure/synthetic-analytics-pipeline/module-warehouse-loader]]"
  - "[[business-logic/synthetic-analytics-pipeline/duplicate-row-prevention-policy]]"
---

# Case 9344 — โหมด Full Refresh ข้าม Row Key Check ทำข้อมูลซ้ำหลุดเข้า Warehouse

## สรุปเคส

ทีมวิเคราะห์พบว่า dataset หนึ่งที่โหลดแบบ `full_refresh` ทุกคืนมีจำนวนแถวมากกว่าจำนวนแถวจริงในต้นทางเกือบสองเท่าหลัง refresh รอบหนึ่ง

## การตรวจสอบ

ตรวจ [[structure/synthetic-analytics-pipeline/module-warehouse-loader]] พบว่าโหมด `full_refresh` ออกแบบมาให้ล้างตารางเป้าหมายทิ้งก่อนโหลดใหม่ทั้งหมด แต่รอบที่มีปัญหาขั้นตอนล้างตารางล้มเหลวเงียบๆ (permission ชั่วคราวไม่พอ) ทำให้การโหลดข้อมูลชุดใหม่ไปต่อทับข้อมูลเก่าที่ยังไม่ถูกล้างแทนที่จะแทนที่ทั้งหมด

## สาเหตุ

logic การตรวจสอบแถวซ้ำด้วย row key ตาม [[business-logic/synthetic-analytics-pipeline/duplicate-row-prevention-policy]] ถูกออกแบบไว้เฉพาะโหมด `append` และ `upsert` เท่านั้น โหมด `full_refresh` ข้ามการเช็คนี้ไปเลยเพราะออกแบบมาโดยสมมติว่าตารางถูกล้างว่างเสมอก่อนโหลด ทำให้เมื่อขั้นตอนล้างตารางล้มเหลว ไม่มีกลไกใดจับ duplicate ได้อีกชั้นหนึ่ง

## การแก้ไข

ล้างตารางที่มีข้อมูลซ้ำด้วยมือแล้วรัน `full_refresh` ใหม่อีกครั้งพร้อมตรวจสอบสิทธิ์การเขียนก่อนเริ่มขั้นตอนล้างตารางทุกครั้ง

## Follow-up

เพิ่มการยืนยันว่าตารางถูกล้างว่างจริงก่อนเริ่มโหลดข้อมูลใหม่ในโหมด `full_refresh` เสมอ (ตรวจนับแถวเป็น 0 ก่อนโหลด) แทนการสมมติว่าขั้นตอนล้างตารางสำเร็จโดยไม่ตรวจสอบซ้ำ
