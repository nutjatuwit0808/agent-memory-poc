---
layer: support-case
tags: [reorder, duplicate, procurement]
created: 2026-05-11
links:
  - "[[structure/synthetic-fleet-maintenance/module-reorder-trigger]]"
  - "[[structure/synthetic-fleet-maintenance/module-parts-inventory]]"
---

# Case 4790 — Reorder trigger สั่งซื้ออะไหล่ชิ้นเดียวกันสองรอบ

## สรุปเคส

Purchasing Manager พบ purchase request สองใบสำหรับ brake pad part ID BP-220 ที่ออกห่างกัน 10 นาทีในวันเดียวกัน ทั้งที่สต็อกต่ำกว่า reorder point เพียงครั้งเดียว

## การตรวจสอบ

ตรวจ [[structure/synthetic-fleet-maintenance/module-reorder-trigger]] พบว่า `stock.below_reorder_point` event ถูกส่งสองครั้งจาก [[structure/synthetic-fleet-maintenance/module-parts-inventory]] เพราะ work order สองใบ deduct stock ห่างกัน 10 นาทีและ deduction แรกทำให้สต็อกต่ำกว่า reorder point พอดี ส่วน deduction ที่สองก็ trigger event อีกครั้ง

## สาเหตุ

reorder-trigger ตรวจ pending request ก่อนสร้างใหม่ แต่ query ใช้ read replica ที่ยังไม่เห็น request ชุดแรกเพราะ replication lag 15 วินาที

## การแก้ไข

ยกเลิก purchase request ซ้ำ แก้ `checkAndTriggerReorder` ให้ query pending request จาก primary database เสมอ

## Follow-up

เพิ่ม idempotency key ในการสร้าง purchase request โดยใช้ `partId + date` เป็น key กัน duplicate แม้จะ query primary แล้ว
