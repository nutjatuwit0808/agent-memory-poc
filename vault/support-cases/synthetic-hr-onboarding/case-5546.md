---
layer: support-case
tags: [task, duplicate]
created: 2025-10-30
links:
  - "[[structure/synthetic-hr-onboarding/module-task-assignment]]"
  - "[[business-logic/synthetic-hr-onboarding/task-duplication-prevention-policy]]"
---

# Case 5546 — Task ซ้ำเพราะ event consumer retry

## สรุปเคส

พนักงานใหม่หลายคนเห็น task "กรอกแบบฟอร์มภาษี" ปรากฏซ้ำกัน 2-3 รายการในหน้า checklist ของตัวเอง สร้างความสับสนว่าต้องทำกี่รอบ

## การตรวจสอบ

ตรวจ log [[structure/synthetic-hr-onboarding/module-task-assignment]] พบว่า `generateTaskList` ถูกเรียกซ้ำหลายครั้งสำหรับ `hireId` เดียวกันในเวลาไล่เลี่ยกัน ตรงกับช่วงที่ event queue มีการ retry message

## สาเหตุ

message consumer ไม่ได้ทำ idempotency check ก่อนประมวลผล event `onboarding.started` — เมื่อ queue ส่ง message ซ้ำ (เพราะ consumer ack ช้ากว่า visibility timeout) ระบบก็สร้าง task ชุดใหม่ทับซ้อนโดยไม่รู้ตัว

## การแก้ไข

เพิ่ม unique constraint ระดับ database บน `(hireId, taskType)` ตาม [[business-logic/synthetic-hr-onboarding/task-duplication-prevention-policy]] แล้ว dedupe task ซ้ำที่มีอยู่แล้วด้วยสคริปต์ครั้งเดียว

## Follow-up

ตรวจสอบ event consumer อื่นในระบบว่ามีความเสี่ยงจากการไม่ทำ idempotency check แบบเดียวกันหรือไม่ โดยเฉพาะตัวที่แตะข้อมูลที่ผู้ใช้เห็นตรงๆ
