---
layer: support-case
tags: [license, drift, audit]
created: 2026-01-09
links:
  - "[[structure/synthetic-asset-management/module-license-pool-manager]]"
  - "[[business-logic/synthetic-asset-management/license-overallocation-policy]]"
---

# Case 6317 — จำนวน License Seat ใน Pool ต่างจาก Vendor จาก Manual Override

## สรุปเคส

ระหว่าง quarterly audit พบว่า license pool ของ productivity suite บันทึกว่ามี seat 150 ตัว แต่ vendor portal แสดง 135 ตัว ทำให้ warning threshold ทำงานผิดเวลา

## การตรวจสอบ

ตรวจ audit log ของ [[structure/synthetic-asset-management/module-license-pool-manager]] พบว่าเมื่อสามเดือนก่อนมีการแก้ตัวเลข seat ผ่าน admin panel โดยตรงโดยไม่ผ่าน `syncLicenseCount` ทำให้ตัวเลขใน pool ไม่ตรงกับ vendor อีกต่อไป

## สาเหตุ

ผู้ดูแลระบบตั้งใจแก้ตัวเลขชั่วคราวระหว่าง license negotiation แต่ลืม sync กลับหลังตกลงจำนวน seat ใหม่เสร็จ ตาม [[business-logic/synthetic-asset-management/license-overallocation-policy]] การ override โดยตรงไม่ควรทำโดยไม่มี followup sync

## การแก้ไข

เรียก `syncLicenseCount` ด้วยตัวเลขจาก vendor portal เพื่อ reset ให้ถูกต้อง พบว่า allocation ปัจจุบัน 128 seat ยังอยู่ในเกณฑ์ที่ซื้อมาจริง ไม่มี overallocation

## Follow-up

ปิด capability การแก้ seat count ด้วยมือผ่าน admin panel — ทุกการเปลี่ยนแปลงต้องผ่าน `syncLicenseCount` ที่มี audit log เท่านั้น
