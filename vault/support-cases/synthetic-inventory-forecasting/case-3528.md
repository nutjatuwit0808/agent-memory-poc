---
layer: support-case
tags: [forecasting, timeout]
created: 2026-05-23
links:
  - "[[structure/synthetic-inventory-forecasting/module-demand-model-runner]]"
  - "[[deployment/synthetic-inventory-forecasting/holiday-capacity-planning-runbook]]"
---

# Case 3528 — Batch พยากรณ์ timeout กลางคันช่วงข้อมูลปริมาณสูงเทศกาล

## สรุปเคส

คืนก่อนเทศกาลใหญ่ batch พยากรณ์ของภูมิภาคที่มี SKU มากที่สุดไม่เสร็จภายในเวลาที่ทีม replenishment ต้องการผลตอนเช้า

## การตรวจสอบ

ตรวจ log [[structure/synthetic-inventory-forecasting/module-demand-model-runner]] พบว่าหลาย shard ใช้เวลาเกิน `MODEL_TIMEOUT_MS` (180 วินาที) เพราะ feature vector ของ SKU ที่มี promo หลายตัวซ้อนกันมีขนาดใหญ่กว่าปกติมาก การคำนวณ blend หลาย promo window พร้อมกันใช้เวลานานกว่าที่ประมาณไว้

## สาเหตุ

timeout ถูกตั้งไว้ตามค่าเฉลี่ยของวันปกติ ไม่ได้คำนึงถึงว่าช่วงเทศกาลจำนวน promo window ที่ซ้อนกันต่อ SKU สูงกว่าปกติหลายเท่า ทำให้ shard ที่มี SKU โปรโมชันหนาแน่นชนกำแพงเวลาเป็นกลุ่ม

## การแก้ไข

รัน `retryFailedShard` ด้วยมือสำหรับ shard ที่ timeout โดยเพิ่ม timeout ชั่วคราวเป็น 2 เท่าเฉพาะรอบนั้น ทำให้ผลลัพธ์เสร็จทันก่อนทีม replenishment เข้างานเช้า

## Follow-up

เสนอปรับ `MODEL_TIMEOUT_MS` ให้ scale ตามจำนวน promo window ที่ overlap ของแต่ละ shard แทนค่าคงที่ตัวเดียว พิจารณาคู่กับ [[deployment/synthetic-inventory-forecasting/holiday-capacity-planning-runbook]]
