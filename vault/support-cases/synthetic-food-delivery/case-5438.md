---
layer: support-case
tags: [eta, outage]
created: 2025-09-28
links:
  - "[[structure/synthetic-food-delivery/module-eta-estimator]]"
  - "[[business-logic/synthetic-food-delivery/driver-acceptance-timeout-policy]]"
---

# Case 5438 — ETA cascade failure จาก traffic data outage

## สรุปเคส

[[structure/synthetic-food-delivery/module-eta-estimator]] เริ่ม return ETA ติดลบและ 0 นาทีสำหรับออร์เดอร์ทุกออร์เดอร์ ทำให้แอปลูกค้าแสดงผิดพลาดและลูกค้าจำนวนมาก cancel ออร์เดอร์ที่ยังไม่ถึงมือ

## การตรวจสอบ

ตรวจ log ของ eta-estimator พบว่า external traffic data API return timeout แล้ว fallback logic คำนวณ `driverToRestaurantMin` เป็นค่า null แทน fallback number จากนั้น null ถูก subtract จากเวลาปัจจุบัน ได้เลขติดลบ

## สาเหตุ

Fallback branch ใน `estimateETA` ตั้งใจจะ assign `FALLBACK_SPEED_KM_PER_HR` แต่ type check ไม่เข้มงวดพอ null จาก API timeout ผ่าน type check แล้วไปถึง arithmetic operation โดยไม่มี guard

## การแก้ไข

Deploy hotfix ที่เพิ่ม null guard ก่อน arithmetic ทุกจุด และตั้งค่า fallback explicitly ใน catch block ของ traffic API call ทั้งหมด

## Follow-up

เพิ่ม integration test สำหรับ traffic API timeout scenario และ add alert สำหรับ ETA ที่ออกมาเป็น ≤ 0 นาทีซึ่งไม่ควรเกิดขึ้น อ้างอิง [[business-logic/synthetic-food-delivery/driver-acceptance-timeout-policy]] สำหรับ ETA boundary ที่ valid
