---
layer: support-case
tags: [intent, model]
created: 2026-07-16
links:
  - "[[structure/synthetic-chat-support-bot/module-intent-classifier]]"
  - "[[deployment/synthetic-chat-support-bot/rollback-procedure]]"
---

# Case 4414 — อัปเดตโมเดลจำแนก Intent ทำ Route ผิดกลุ่มจำนวนมาก

## สรุปเคส

หลังอัปเดตโมเดลจำแนก intent เวอร์ชันใหม่ ทีม support สังเกตว่าคำถามเกี่ยวกับการยกเลิกบริการถูกจำแนกเป็นคำถามทั่วไปบ่อยผิดปกติ ทำให้ไม่ได้ถูกส่งต่อเจ้าหน้าที่ตามที่ควร

## การตรวจสอบ

ตรวจ [[structure/synthetic-chat-support-bot/module-intent-classifier]] พบว่า label `cancellation` มีสัดส่วนลดลงชัดเจนหลัง deploy โมเดลใหม่ เทียบกับช่วงก่อนหน้า

## สาเหตุ

ข้อมูลฝึกโมเดลเวอร์ชันใหม่ปรับสัดส่วนตัวอย่างของแต่ละ label ใหม่เพื่อแก้ปัญหาอื่น แต่ลดสัดส่วนตัวอย่าง `cancellation` ลงโดยไม่ตั้งใจ ทำให้โมเดลมั่นใจ label นี้น้อยลงกว่าความเป็นจริง

## การแก้ไข

rollback โมเดลกลับเวอร์ชันก่อนหน้าทันทีตาม [[deployment/synthetic-chat-support-bot/rollback-procedure]] แล้วให้ทีม data ตรวจสอบสัดส่วนข้อมูลฝึกก่อนจะ deploy โมเดลใหม่รอบถัดไป

## Follow-up

เพิ่ม automated check เปรียบเทียบสัดส่วนผลจำแนกแต่ละ label ของโมเดลใหม่กับโมเดลเดิมก่อน deploy จริง แจ้งเตือนถ้าต่างกันเกิน threshold ที่กำหนด
