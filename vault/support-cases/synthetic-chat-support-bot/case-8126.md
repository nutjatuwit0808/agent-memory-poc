---
layer: support-case
tags: [state, bug, race-condition]
created: 2025-12-11
links:
  - "[[structure/synthetic-chat-support-bot/module-conversation-state-manager]]"
---

# Case 8126 — Turn สองอันเขียนทับกันพร้อมกันทำประวัติบทสนทนาสลับลำดับ

## สรุปเคส

ทีม QA พบว่าบางบทสนทนามีลำดับข้อความในประวัติสลับกัน (คำตอบของ bot ปรากฏก่อนคำถามของลูกค้าที่ถามจริง) ทำให้ดูประวัติแล้วสับสน

## การตรวจสอบ

ตรวจ `appendTurn` ของ [[structure/synthetic-chat-support-bot/module-conversation-state-manager]] พบว่ามี race condition เมื่อสอง turn ของบทสนทนาเดียวกันถูกเขียนพร้อมกันในเวลาไล่เลี่ยกันมาก (เช่น bot ตอบเร็วมากจนชนกับข้อความถัดไปของลูกค้า)

## สาเหตุ

การกำหนดลำดับ turn ใช้ timestamp ของ application server แทนที่จะใช้ sequence number ที่ database รับประกัน ทำให้ในบางกรณีที่ clock ของ instance คนละตัวเขียนพร้อมกัน ลำดับที่บันทึกไม่ตรงกับลำดับเวลาจริงที่เกิดขึ้น

## การแก้ไข

แก้ให้ `appendTurn` ใช้ auto-increment sequence number ระดับ database แทน timestamp ของ application ในการกำหนดลำดับ แล้ว deploy เป็น hotfix

## Follow-up

ตรวจสอบฟังก์ชันอื่นใน [[structure/synthetic-chat-support-bot/module-conversation-state-manager]] ที่พึ่งพา timestamp ของ application server ในการกำหนดลำดับเหตุการณ์ว่ามีความเสี่ยงเดียวกันหรือไม่
