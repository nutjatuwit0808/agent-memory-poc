---
layer: support-case
tags: [serialization, bug]
created: 2026-01-15
links:
  - "[[structure/synthetic-ad-bidding/module-bid-request-handler]]"
  - "[[deployment/synthetic-ad-bidding/ci-cd-pipeline]]"
---

# Case 8741 — Bid Request Handler ดรอปคำขอจำนวนมากจาก Schema ไม่ตรงกัน

## สรุปเคส

อัตรา request ที่ถูกดรอปโดยไม่มีเหตุผลชัดเจนพุ่งขึ้นกะทันหันหลัง deploy service เวอร์ชันใหม่ตอนดึก

## การตรวจสอบ

ตรวจ log [[structure/synthetic-ad-bidding/module-bid-request-handler]] พบ deserialization error จำนวนมากตอนแปลง internal bid object ที่ auction-engine ส่งมา เป็น field ที่ handler เวอร์ชันใหม่ไม่รู้จัก

## สาเหตุ

ทีมเพิ่ม field ใหม่ใน schema ของ internal bid object ฝั่ง auction-engine แล้ว deploy ก่อน แต่ bid-request-handler ยังเป็นเวอร์ชันเก่าที่ compile ด้วย schema เก่า — schema เปลี่ยนแบบ breaking (ไม่ backward compatible) ทั้งที่ตั้งใจให้เป็น optional field

## การแก้ไข

rollback auction-engine กลับ schema เดิมชั่วคราว แล้ว deploy bid-request-handler เวอร์ชันที่รองรับ schema ใหม่ให้เสร็จก่อน จึง deploy auction-engine เวอร์ชันใหม่อีกครั้งตามลำดับที่ถูกต้อง

## Follow-up

กำหนดลำดับการ deploy ที่ชัดเจนสำหรับ service ที่แชร์ schema กัน (deploy consumer ก่อน producer เสมอ) เพิ่มเข้า [[deployment/synthetic-ad-bidding/ci-cd-pipeline]]
