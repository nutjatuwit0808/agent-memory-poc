---
layer: support-case
tags: [engagement, bug]
created: 2026-05-01
links:
  - "[[structure/synthetic-social-feed/module-engagement-tracker]]"
  - "[[business-logic/synthetic-social-feed/engagement-dedup-policy]]"
---

# Case 4431 — Like ถูกนับซ้ำจาก Retry Logic ของ Client

## สรุปเคส

ทีม data science สังเกตว่าจำนวน like ของโพสต์บางกลุ่มสูงผิดปกติเทียบกับ comment/share ที่ proportion ปกติ

## การตรวจสอบ

ตรวจ [[structure/synthetic-social-feed/module-engagement-tracker]] พบว่า mobile app เวอร์ชันใหม่มี retry logic ที่ยิง request like ซ้ำถ้าไม่ได้ response ภายใน 500ms โดยไม่ส่ง request ID เดิมมาด้วย

## สาเหตุ

dedup key ของ [[business-logic/synthetic-social-feed/engagement-dedup-policy]] ออกแบบมาให้ใช้ request ID เป็นหลัก แต่ client เวอร์ชันใหม่ generate request ID ใหม่ทุกครั้งที่ retry แทนที่จะใช้ตัวเดิม ทำให้ dedup ไม่ทำงาน

## การแก้ไข

แก้ mobile app ให้ retry ด้วย request ID เดิมเสมอ (hotfix เร่งด่วน) พร้อมรัน job แก้ไขจำนวน like ที่นับซ้ำย้อนหลังในช่วงที่ได้รับผลกระทบ

## Follow-up

เพิ่ม fallback dedup ฝั่ง server โดยใช้ (userId, postId, timestamp window) ควบคู่กับ request ID ไม่พึ่ง client ส่ง ID ที่ถูกต้องมาเพียงอย่างเดียว
