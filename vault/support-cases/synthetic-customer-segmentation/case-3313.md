---
layer: support-case
tags: [lifecycle, archive, incident]
created: 2026-02-06
links:
  - "[[structure/synthetic-customer-segmentation/module-segment-builder]]"
  - "[[business-logic/synthetic-customer-segmentation/segment-archival-policy]]"
---

# Case 3313 — Segment ถูก Archive โดยไม่ได้ตั้งใจจาก False Positive ของ Inactive Detection

## สรุปเคส

campaign manager พบว่า segment ที่ใช้สำหรับ seasonal campaign ถูก archive ไปแล้ว ทำให้ export ที่ schedule ไว้ล้มเหลวทั้งหมด

## การตรวจสอบ

ตรวจ archival log ของ [[structure/synthetic-customer-segmentation/module-segment-builder]] พบว่า segment ถูก auto-flag เป็น inactive เพราะไม่มี export เกิน 90 วัน ทั้งที่ campaign ถูก plan ให้ start หลังจากนั้น 30 วัน owner ยืนยัน archive flag แบบไม่ได้ตั้งใจเพราะคิดว่าเป็น notification ทั่วไป

## สาเหตุ

email notification ที่ส่งให้ owner ก่อน archive ไม่ชัดเจนว่าการ confirm คืนการยืนยัน archive จริงๆ ไม่ใช่แค่รับทราบ ทำให้เกิดความเข้าใจผิด

## การแก้ไข

restore segment จาก archive (definition ยังคงอยู่ตาม [[business-logic/synthetic-customer-segmentation/segment-archival-policy]]) และ trigger manual refresh และ export เพื่อไล่ทัน

## Follow-up

เปลี่ยน archive confirmation flow ให้ require explicit action มากกว่าแค่ click email link และเพิ่ม `planned_reactivation_date` field ให้ owner ระบุได้ว่า segment นี้จะกลับมาใช้เมื่อไหร่
